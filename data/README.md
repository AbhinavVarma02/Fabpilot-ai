# Data

Raw and processed datasets are not committed to this repository.

Expected local layout:

```text
data/
  raw/
    secom/
      secom.data
      secom_labels.data
      secom.names
    wm811k/
      LSWMD.pkl
  processed/
```

Use the attached dataset archives for local setup. Do not download datasets again unless the attached files are missing or corrupted.
